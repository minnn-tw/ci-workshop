format_version: 9
pipelines:
  01-HelloWorld:
    group: MyGroup
    materials:
      repository:
        git: git@github.com:wanichnun-tw/ci-workshop.git
        branch: master
    stages:
      - test:
          clean_workspace: true
          jobs:
            run-unit-test:
              tasks:
                - exec:
                    arguments:
                      - run
                      - hello-world
                    command: docker
